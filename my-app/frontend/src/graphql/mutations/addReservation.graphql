mutation ($startDate: String!, $endDate: String!){
  addReservation(
    endDate: $endDate
    startDate: $startDate
  ){
    endDate
    startDate
    id
    reservedBy {
      id
      name
    }
  }
}